import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  message: {
    padding: 10,
    marginBottom: 8,
    backgroundColor: '#eee',
    borderRadius: 8,
  },
  meta: {
    fontSize: 12,
    color: '#666',
  },
});